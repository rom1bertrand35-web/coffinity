const { createClient } = require('@supabase/supabase-js'); const path = require('path'); require('dotenv').config({ path: path.resolve(__dirname, '../.env.local') }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function seed() { const now = new Date(); const onejan = new Date(now.getFullYear(), 0, 1); const week = Math.ceil((((now.getTime() - onejan.getTime()) / 86400000) + onejan.getDay() + 1) / 7); const { data: coffees } = await supabase.from('coffees').select('id, name').limit(3); if (!coffees || coffees.length < 3) return; const selection = { week_number: week, year: now.getFullYear(), title: 'Éveil de Printemps', description: 'Une sélection de crus légers et floraux pour accompagner les premiers rayons de soleil. Idéal pour un réveil en douceur.', coffee_ids: coffees.map(c => c.id) }; const { error } = await supabase.from('weekly_selections').upsert(selection, { onConflict: 'week_number,year' }); if (error) console.error(error); else console.log('Weekly selection seeded.'); } seed();
