const { createClient } = require('@supabase/supabase-js'); const path = require('path'); require('dotenv').config({ path: path.resolve(__dirname, '../.env.local') }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function check() { const { count } = await supabase.from('coffees').select('*', { count: 'exact', head: true }); console.log('Count:', count); const { data } = await supabase.from('coffees').select('name, brand').limit(5); console.log('Samples:', data); } check();
