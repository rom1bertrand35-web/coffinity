const { createClient } = require('@supabase/supabase-js'); const path = require('path'); require('dotenv').config({ path: path.resolve(__dirname, '../.env.local') }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function clean() { const { data } = await supabase.from('coffees').select('id, name, image_url'); const toDelete = data.filter(c => !c.image_url || c.image_url.includes('Inconnu') || c.image_url.length < 10); console.log('Found', toDelete.length, 'bad refs to delete'); if (toDelete.length > 0) { await supabase.from('coffees').delete().in('id', toDelete.map(d => d.id)); } } clean();
