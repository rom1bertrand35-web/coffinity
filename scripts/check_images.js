const { createClient } = require('@supabase/supabase-js'); const path = require('path'); require('dotenv').config({ path: path.resolve(__dirname, '../.env.local') }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function checkImages() { const { data, error } = await supabase.from('coffees').select('name, brand, image_url').limit(150); if (error) console.error(error); else { console.log('Total coffees:', data.length); const withoutImage = data.filter(c => !c.image_url); console.log('Without image:', withoutImage.length); const samplesWithImage = data.filter(c => c.image_url).slice(0, 10); console.log('Samples with image:', samplesWithImage); } } checkImages();
