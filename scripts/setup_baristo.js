const { createClient } = require('@supabase/supabase-js'); const path = require('path'); require('dotenv').config({ path: path.resolve(__dirname, '../.env.local') }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function setupBaristo() { const baristoId = '00000000-0000-0000-0000-000000000000'; const config = { skinColor: '#F3D2B3', hairStyle: 'cap', facialHair: 'mustache', facialHairColor: '#1A1A1A', clothing: 'barista_apron_over_tee', clothingColor: '#FAFAF8', expression: 'wide_smile_teeth', accessory: 'none' }; const { data: existing } = await supabase.from('profiles').select('id').eq('username', 'Baristo').maybeSingle(); if (!existing) { console.log('Creating Baristo profile...'); const { error } = await supabase.from('profiles').insert({ id: baristoId, username: 'Baristo', avatar_url: '🧞‍♂️', avatar_config: config, level: 'Maître Barista', points: 9999, has_onboarded: true }); if (error) console.error(error); else console.log('Baristo created.'); } else { console.log('Baristo already exists.'); await supabase.from('profiles').update({ avatar_config: config }).eq('username', 'Baristo'); } } setupBaristo();
