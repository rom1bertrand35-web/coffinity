const { createClient } = require('@supabase/supabase-js'); const path = require('path'); require('dotenv').config({ path: path.resolve(__dirname, '../.env.local') }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function check() { const { data, error } = await supabase.rpc('get_table_columns', { table_name: 'profiles' }); if (error) { const { data: data2, error: error2 } = await supabase.from('profiles').select('*').limit(1); console.log('Sample row:', data2); } else { console.log('Columns:', data.map(c => c.column_name)); } } check();
