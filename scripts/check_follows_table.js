const { createClient } = require('@supabase/supabase-js'); const path = require('path'); require('dotenv').config({ path: path.resolve(__dirname, '../.env.local') }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function checkFollows() { try { const { data, error } = await supabase.from('follows').select('*').limit(1); if (error) console.error('Error fetching follows:', error); else console.log('Follows columns:', data.length > 0 ? Object.keys(data[0]) : 'Empty table'); } catch(e) { console.error('Catch error:', e); } } checkFollows();
